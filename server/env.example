# Google Cloud Vision API Configuration
# One of these two approaches:
# 1) Point to your JSON credentials file (recommended)
GOOGLE_APPLICATION_CREDENTIALS=./server/service-account.json

# 2) Or specify a path we pass into the client constructor
# GCP_SERVICE_ACCOUNT_JSON=./server/service-account.json

# Server Configuration
PORT=8787
NODE_ENV=development
